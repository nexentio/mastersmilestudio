'use client';

import React from 'react';
import styles from './ZirconiumCrownsDetailView.module.css';

export default function ZirconiumCrownsDetailView() {
  return (
    <div className={styles.wrapper}>
      {/* Clean canvas - ready for sections */}
    </div>
  );
}
